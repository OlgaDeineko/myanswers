function categoryHelper() {

  let responseToData = (cat) => {

    return cat;
  };

  let dataToRequest = (cat) => {

    return cat
  };

  return {
    responseToData,
    dataToRequest
  }
}

export default categoryHelper();
