import template from './visitor.html';
import controller from './visitor.controller';
import './visitor.scss';

let visitorComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default visitorComponent;
